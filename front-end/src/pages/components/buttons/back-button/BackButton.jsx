import * as S from "./ButtonStyle";

export function BackButton() {
    return (
            <S.StyledBackButton to= "/">
                <S.ArrowBackIcon>
                </S.ArrowBackIcon>
            </S.StyledBackButton>
    );
}