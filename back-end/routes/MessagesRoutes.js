const { Games } = require("../config/db");
//modules
const express = require("express");
const router = express.Router();

router.post("/cadaster", async (req, res) => { /* lógica de cadastro */ });

module.exports = router;